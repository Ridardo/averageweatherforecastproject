<html>
<style type="text/css">
   #footer {
    position: fixed; /* Фиксированное положение */
    left: 0; bottom: 0; /* Левый нижний угол */
    padding: 10px; /* Поля вокруг текста */
    background: #39b54a; /* Цвет фона */
    color: #fff; /* Цвет текста */
    width: 100%; /* Ширина слоя */
   }
  </style>
<body>
<center>
<div>
    <form action="/logout" method="post">
        <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
    <input type="submit" value="Выйти из аккаунта"/>
    </form>
</div>
<div>
    <form method="post">
        <input type="text" name="townInput" placeholder="Введите город" />
        <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
        <button type="submit">Запросить погоду</button>
    </form>
</div>

{{#towns}}
    <div>Результаты запроса:</div>
    <div>-----------------------------------------------</div>
    <div>
        <span>Прогноз на завтра в городе <b>{{name}}</b>: <u>{{future}}</u></span>
        <div>-----------------------------------------------</div>
        <div>
            Погода в данный момент:
        </div>
        <i><div>{{#forecastss}}
                <div>
                    <span>{{degrees}}℃, </span>
                    <span>ветер {{wind}} м/с, </span>
                    <span>влажность {{humidity}}%, </span>
                    <span>давление {{pressure}} мм.р.с., </span>
                    <span>датировано {{date}}</span>
                </div>
            {{/forecastss}}</div></i>
    </div>
    <div>-----------------------------------------------</div>
{{/towns}}

<a href="/archive">Поиск прогнозов</a>
    <div id="footer"> Информация о погоде предоставлена сервисами: Yandex, OpenWeatherMap, WeatherAPI</div>
</center>
</body>
</html>